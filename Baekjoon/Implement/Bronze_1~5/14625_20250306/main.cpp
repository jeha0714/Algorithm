#include <iostream>
#include <vector>

int main(void)
{
	int startH, startM, endH, endM;
	int N;
	int appearTime = 0;

	// insert input value
	std::cin >> startH >> startM >> endH >> endM >> N;

	// calculate
		// check over hour
	if (startH + 1 < endH)

	// print result
	std::cout << appearTime;

	return (0);
}